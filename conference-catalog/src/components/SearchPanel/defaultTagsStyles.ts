import {COLORS} from "../../constants";

const defaultTagsStyles = {
    root:{
        backgroundColor:COLORS.MERCURY,
        height:50,
        marginBottom:10,
    }
};
export default defaultTagsStyles;
